<?xml
    version = "1.0"
    encoding = "utf-8" ?>

<ResourceDictionary xmlns="http://xamarin.com/schemas/2014/forms"
                    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
                    xmlns:simpleImageButton="clr-namespace:SimpleImageButton;assembly=SimpleImageButton"
                    x:Class="SimpleImageButton.SimpleImageButtonResources">

    <!-- Style for the button layout -->
    <Style x:Key="SimpleImageButtonLayoutStyle"
           TargetType="simpleImageButton:SimpleImageButton">
        <Setter Property="CornerRadius" Value="12" />
        <Setter Property="BackgroundColor" Value="Fuchsia" />

    </Style>

    <!-- Style for the label component -->
    <Style x:Key="SimpleImageButtonLabelStyle"
           TargetType="Label">
        <Setter Property="FontSize" Value="33" />
        <Setter Property="FontFamily" Value="Helvetica" />
        <Setter Property="BackgroundColor" Value="Orange" />
    </Style>

    <!-- Style for the image component -->
    <Style x:Key="SimpleImageButtonImageStyle" TargetType="Image">
        <Setter Property="Margin" Value="0" />
        <Setter Property="VerticalOptions" Value="Center" />
        <Setter Property="HorizontalOptions" Value="Center" />
        <Setter Property="Aspect" Value="AspectFill" />
        <Setter Property="HeightRequest" Value="55" />
        <Setter Property="WidthRequest" Value="55" />
    </Style>

</ResourceDictionary>